# 상세 화면(DetailPage) UI 리팩토링

대상 파일: `frontend/src/pages/DetailPage.tsx`, `DetailPage.module.css`

## 전제 (바꾸지 않는 것)
- 컬러 토큰: Primary `#088bff`, Secondary `#f55220` — 이미 확정, 전 페이지 공통이므로 유지.
- 전역 폰트(시스템 폰트 스택) — 상세 화면만 별도 서체 적용 시 다른 페이지와 톤이 어긋남. 폰트 교체는 이 문서 범위 밖(전역 결정 필요).
- 레이아웃 골격(좌 정보 / 우 예보 2컬럼, sticky) — 구조 자체는 문제없음, 톤만 손봄.

## Stage 0 — 코드 정리 (완료)
`DetailPage.module.css`에 있던 중복/죽은 규칙 제거:
- `.festivalNotice`/`.festivalItem`/`.festivalDot`가 두 번 정의되어 있던 것 → 하나로 통합
- 실제 JSX에서 안 쓰는 클래스 삭제: `.tag`, `.tagRow`, `.dateRangeLabel`, `.selectedRangeLabel`, `.weekSectionLabel`, `.weekDateRange`, `.weekDateRangeSub`, `.dayCellFestival`(+hover 툴팁), `.festivalItemIcon/Name/Period`

## Stage 1 — 아이콘 통일 (완료)
**문제**: 뒤로가기·주차장 버튼은 커스텀 SVG인데, 나머지(📍 위치 / 🎉 축제 / 🔄 새로고침 / ⚠️ 경고)는 이모지 — 톤이 섞여 있음.

**변경**
- 파일 상단에 `IconPin`/`IconFestival`/`IconRefresh`/`IconAlert` 4개 SVG 컴포넌트 추가 (stroke-based, `currentColor` 상속 → 배치된 위치의 텍스트 색을 그대로 따라감)
- `spot.address` 앞 📍 → `IconPin`
- 축제 배너/툴팁의 🎉 (2곳) → `IconFestival` (secondary 컬러)
- `btnRefresh`의 🔄 → `IconRefresh`
- `alertBanner`의 ⚠️ → `IconAlert`
- 추가로 추천 카드의 `부산시 {구}` 위치 텍스트에도 `IconPin` 적용 — 상세 화면 주소와 카드 위치 표기가 같은 아이콘 언어를 쓰도록 통일 (`recCardLoc`)

파일: `DetailPage.tsx`, `DetailPage.module.css`

## Stage 2 — 히어로 영역에 "눈치게임" 컨셉 강화 (완료)
**문제**: 서비스의 핵심 아이덴티티(레벨 이미지 + "눈치성공/눈치실패" 카피)가 사진 우측 상단의 작은 뱃지(44px) 하나뿐. 정작 상세 페이지에서 가장 안 보임. 레벨 이미지 자체가 이미 "눈치게임 성공!/실패!" 문구가 박힌 원형 스티커 일러스트라 확대 노출만으로도 시그니처가 됨.

**적용한 방향**
- 레벨 뱃지를 title row에서 빼서, 사진 우측 하단 모서리에 걸쳐지는 "오늘의 눈치게임 결과 스탬프"로 확대 배치 (100px, -6deg 회전 + drop-shadow로 스티커가 붙은 느낌)
- 스탬프 아래에 오늘 혼잡도 `{score}%`를 레벨 컬러로 보여주는 작은 필 라벨 추가 — 뱃지가 "결과"를, 숫자가 "근거"를 담당
- `spotImageWrap`(새 래퍼)이 오버플로된 스탬프 공간을 확보, 모바일에서는 76px로 축소

파일: `DetailPage.tsx`(spotImage 블록), `DetailPage.module.css`

## Stage 3 — 제네릭 컴포넌트 재설계 (완료)
**문제**: 아래 요소들이 어느 서비스에서나 볼 수 있는 기본값으로 되어 있음.
- `.tabGroup`/`.tabBtn`: `#f0f0f0` + 흰 알약 iOS 세그먼트 컨트롤 그대로
- `.recCard::before`: 카드 상단 4px 그라디언트 바 — 의미 없는 장식
- `.alertBanner`: 이모지 아이콘 + 틴트 배경의 표준 warning banner

**적용한 방향**
- 탭: 회색 필 세그먼트 → 밑줄형(primary 컬러 underline)으로 교체, 배경/그림자 제거
- 카드 상단 그라디언트 바 삭제, 대신 코너의 레벨 뱃지(`recCardLevelImg`)에 -4deg 회전을 줘 Stage 2 히어로 스탬프와 같은 "스티커" 시각 언어로 통일
- 경고 배너: `IconAlert`(범용 경고 삼각형) 제거 → 해당 스팟의 실제 레벨 뱃지 이미지를 42px로 넣어 "왜 추천하는지"를 그 자리에서 시각적으로 증명하도록 변경

파일: `DetailPage.tsx`, `DetailPage.module.css`

## Stage 4 — 이번주/다음주 그리드 구분 명확화 (완료)
**문제**: `WeekGrid`가 이번 주/다음 주에서 완전히 동일한 톤이라 `.divider` + 라벨 하나로만 구분됨. 두 구간이 다른 이유(선택 가능 vs 참고용 등)가 시각적으로 안 드러남.

**적용한 방향**
- "이후 7일" 그리드를 `--bg-page` 톤의 둥근 패널로 감싸 이번 주(선택 가능)와 구분
- 좌우 대칭 divider 대신 좌측 정렬 라벨 + "선택한 기간 다음에 자동으로 표시돼요" 힌트 텍스트로 교체 — 왜 구분되는지 설명

파일: `DetailPage.tsx`, `DetailPage.module.css`

## 추가 — 예측 데이터 폴백 UI (완료)
PRD상 미착수 항목("API 장애 폴백 UI")과 맞물려 있던 이슈. 기존엔 forecast 쿼리에 `isError`를 아예 안 봐서, API 실패 시 `thisWeek`/`nextWeek`가 빈 배열이 되어 그리드 자리에 아무 설명 없이 텅 비어 보였음.

- API 실패(`isError`): "예측 혼잡도를 불러오지 못했어요" + 다시 시도 버튼(`refetch`)
- API는 성공했지만 `selectedPeriod`/`followingPeriod` 둘 다 데이터가 없는 경우: "이 관광지는 아직 예측 데이터가 없어요" (재시도 버튼 없음 — 재시도해도 같은 결과이므로)
- 기존에 있던 날짜 단위 부분 공백(`padToSeven`의 "—" 폴백)은 이미 정상 동작이라 손대지 않음

파일: `DetailPage.tsx`, `DetailPage.module.css`

## Stage 5 — 예측 혼잡도·축제 정보 재설계 (2026-08-11 예측 혼잡도 A 전체 적용, 축제 정보 B는 디자인 유지)
출처: Claude Design 프로젝트 [혼잡도 리스트](https://claude.ai/design/p/9d183d43-8c26-4000-b3cb-24fb66a1feda?file=%ED%98%BC%EC%9E%A1%EB%8F%84+%EB%A6%AC%EC%8A%A4%ED%8A%B8.dc.html) 옵션 `2f`(전체 화면 — 예측 혼잡도 14일 + 축제 정보 향후 30일, 달력 밴드 ↔ 리스트 상호 하이라이트). 전체를 한 번에 옮기지 않고 항목 단위로 선택 적용하기로 함 — 아래는 적용 여부 결정 전 정리한 항목 목록.

**A. 예측 혼잡도(캘린더) 쪽**

| # | 항목 | 현재 상태 | 비고 |
|---|---|---|---|
| 1 | [x] (2026-08-11) 유의 문구를 캘린더 섹션 최상단으로 이동 | "예측 혼잡도" 타이틀 바로 아래 설명(desc)처럼 배치, 기간 선택 버튼은 오른쪽 상단 정렬 유지. 축제 카드에서는 제거 | `.festivalCaution`→`.forecastCaution`으로 이름도 정리(더 이상 축제 전용 아님). 처음엔 헤더 행 아래 독립된 줄로 넣었다가, 타이틀과 묶이지 않고 붕 떠 보인다는 피드백으로 타이틀+캡션을 한 컬럼으로 재배치 |
| 2 | [x] (2026-08-11, 설계 변경) 축제 표시 | ~~밴드~~ 대신 카드 우상단 깃발(⚑, `.dayFestivalFlag`)로 단순화 | Claude Design 쪽에서 2f 자체가 개정되면서 "밴드" 안이 폐기되고 4a 방식(코너 깃발)으로 통일됨 — 우리도 그에 맞춰 밴드 없이 구현 |
| 3 | [x] (2026-08-11) 캘린더 ↔ 축제 리스트 상호 하이라이트 | `highlightedFestivalId` state를 `DetailPage`로 끌어올려 `WeekGrid`(양쪽 주)·하단 축제 리스트·날짜 팝업 안 "이 날의 축제" 미니 리스트 3곳이 공유. 어느 쪽을 hover해도 같은 축제의 나머지가 `--color-secondary-tint`/`--color-secondary`로 강조 | 자기 자신의 팝업 안 항목을 hover하는 경우 `.dayCell:hover`(레벨색 보더)와 `.dayCellHighlighted`(강조색 보더)가 같은 요소에 동시 적용될 수 있어, 후자를 `.dayCell.dayCellHighlighted`(클래스 2개, specificity 동률)로 만들고 소스 순서를 뒤에 둬서 강조가 이기도록 처리 |
| 4 | [x] (2026-08-11) "★ 추천" 배지 — 가장 한가한 날에 표시 | `thisWeek`+`nextWeek` 통합 최소 `rate` 계산, `WeekGrid`에 `recommendedDate` prop으로 전달. 배경색은 2f와 동일하게 `--level-1`(파랑) 재사용(기존엔 `--color-primary`였음) | 원안은 "14일 중"이었지만 실제로는 14일이 보장 안 됨(1행 = 선택 기간이라 7일 미만일 수 있고, 2행 = 30일 예측 끝자락에서 partial일 수 있음) — `rate !== null`인 날짜만 걸러 비교하도록 수정. 날짜 문자열(`monthDay`, 두 행에서 겹치지 않음)로 매칭해 인덱스 계산 없이 단순화 |
| 5 | [x] (2026-08-11) 날짜 hover 팝업에 공감 멘트 추가 | `LEVEL_TIP` 상수(레벨별 5문장, 2f `SAY` 그대로 이식)를 `tooltipSay`로 표시 | — |
| 6 | [x] (2026-08-11) 팝업 안에 "축제 기간엔 예측과 실제가 더 차이날 수 있어요" 주의문 추가 | `tooltipFestivalCaution`, 축제 있는 날에만 노출 | 기존엔 단일 축제 1줄만 보여줬는데, 2f가 `d.festivals` 배열 전체를 "이 날의 축제" 리스트로 보여주도록 개정돼 있어 우리도 함께 다건 지원으로 업그레이드(원래 `DayEntry.festivals`가 이미 배열이라 데이터 구조 변경은 불필요) |
| 11 | [x] (2026-08-11) 날짜 셀(`dayCell`) 레이아웃 정비 (원래 체크리스트엔 없었지만 2f 참고해 함께 진행) | 요일+날짜를 세로 2줄 → 한 줄 양끝 정렬로 압축, 항상 보이는 혼잡도 `%` 숫자 추가(이전엔 hover 팝업에서만 확인 가능), 카드 테두리를 hover 때만 보이던 것에서 평상시에도 옅게(`--color-border`) 보이도록, radius 10→14px | 2f는 스탬프 이미지 자리를 dashed 원형 placeholder("캐릭터" 텍스트)로 표현했지만, 우리는 이미 원형 스티커 일러스트가 있어 그 부분은 그대로 둠(원+원 이중 테두리 방지) — "2f 다 적용" 요청 이후에도 이 판단은 유지, 필요하면 별도로 다시 논의 |
| 12 | [x] (2026-08-11) 레벨 라벨 텍스트 색상 + 셀 배경/보더를 2f와 동일하게 | `dayLevelLabel`을 고정 진한색 → 그 날의 레벨색(인라인, `--cell-accent`로 hover 보더에도 재사용)으로 되돌림 | 8/11 새 팔레트로 흰 배경 대비 재계산: 1 3.93:1, 2 2.82:1, 3 2.02:1, 4 2.34:1, 5 3.66:1(WCAG AA 텍스트 기준 4.5:1) — 새 팔레트가 이전보다는 낫지만 10px 굵은 글씨엔 전부 미달, 특히 레벨3·4가 약함. 축제 있는 날 배경/보더 상시 틴트는 이후 2f 개정판에서 제거되고(코너 깃발만 남음, §2) 상호 하이라이트 때만 틴트되도록 바뀌어 반영(§3) |

**B. 축제 정보(하단 리스트) 쪽**

| # | 항목 | 현재 상태 | 비고 |
|---|---|---|---|
| 7 | 타이틀 옆 "향후 30일 · 날짜범위" 서브카피 | 없음 | 텍스트만 추가 |
| 8 | "달력에 표시된 축제에 마우스 올리면 강조돼요" 안내문 | — | 3번을 적용해야 의미 있음(3번 종속) |
| 9 | 항목마다 "달력에 표시됨 / 예측 범위 이후" 태그 | 없음 — 지금은 다 동일하게 표시 | 축제 기간이 14일 예보 범위 안/밖인지 계산 |
| 10 | 리스트를 3열 그리드(아이콘칩+이름/장소 · 날짜 pill · 화살표)로 재배치 | 지금은 세로 스택 티켓 카드 | 우리 점선 날짜 pill이랑 톤은 이미 비슷함 |

의존관계는 8→3 하나뿐, 나머지는 독립적으로 선택 가능. 어떤 번호를 적용할지 정해지면 그것만 구현.

---
## 진행 방식
Stage 1개씩 순서대로 확인받고 적용. 4개 Stage + 예측 데이터 폴백까지 완료, Stage 5는 항목 선택 대기 중.
